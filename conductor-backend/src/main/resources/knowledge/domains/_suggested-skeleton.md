---
type: schema
title: "%DOMAIN_DISPLAY% domain schema"
description: "Page-type taxonomy, path layout, and body templates for the %DOMAIN_DISPLAY% domain."
---

# %DOMAIN_DISPLAY% domain schema

This domain was approved from a librarian-raised gap report, so this page starts as a skeleton — there's
no page-type taxonomy defined yet. **Define one before filing anything here**: read the root
[Knowledge Center schema](/_schema.md) for the frontmatter contract and conventions this page should
follow, and look at an existing domain's schema (e.g. `engineering/_schema.md`) for the shape a domain
schema takes — a taxonomy table, a path layout list, and a body template per type.

## Page-type taxonomy

*(Not yet defined. Add a table here: `type` → what it's for, once you know what kinds of pages this
domain needs.)*

## Path layout

*(Not yet defined. Add the subdirectories under `%DOMAIN_SLUG%/` each type files into, e.g.
`%DOMAIN_SLUG%/<subdir>/` — one per type above.)*

## Body templates

*(Not yet defined. Add one template per type — the stable section structure every page of that type
should follow, matching the format of the other domain schemas.)*

## Create vs. edit

Same rule as the root schema: edit in place for an update to something a page already covers; create a
new page only for a genuinely distinct entity. Check `index.md` and `search_knowledge` first — a
near-miss title is a strong signal an existing page already owns this entity.
