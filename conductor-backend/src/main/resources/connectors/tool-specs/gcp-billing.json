{
  "description": "Cloud spend by service from GCP BigQuery billing export",
  "operations": [
    {
      "id": "cost_by_service",
      "description": "Cloud spend per GCP service: current 30d vs prior 30d with MoM delta percentage",
      "params": {},
      "outputShape": "{services:[{service,cost,currency}], totalCost:number, previousPeriodCost:number, momDeltaPct:number, currency:string}",
      "outputKeys": ["services", "totalCost", "previousPeriodCost", "momDeltaPct", "currency"]
    }
  ]
}
