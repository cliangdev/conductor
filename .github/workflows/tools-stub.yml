name: Tools CI (no-op)

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - 'conductor-tools/**'

jobs:
  test-cli:
    name: tools
    runs-on: ubuntu-latest
    steps:
      - run: echo "No conductor-tools/** changes; skipping real CI."
